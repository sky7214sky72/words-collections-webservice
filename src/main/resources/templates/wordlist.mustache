{{>layout/header}}
<br/>
<!--<div class="col-12">
    <select name="demo-category" id="word-category">
        <option value="">- 파트선택 -</option>
        #posts}}
        <option value="category}}">category}}</option>
        /posts}}
    </select>
</div>-->
<h4>단어 목록</h4>
<br/>
<div class="table-wrapper">
    <table class="alt">
        <thead>
        <tr>
            <th>단어</th>
            <th>의미</th>
        </tr>
        </thead>
        <tbody id="toeicList">
        {{#posts}}
        <tr>
            <td><p name="word">{{word}}</p></td>
            <td><p name="meaning">{{meaning}}</p></td>
        </tr>
        {{/posts}}
        </tbody>
    </table>
    {{#pageCount}}
        {{#pageNumber}}
            <h4>현재 페이지 번호 : {{pageNumber}}</h4>
        {{/pageNumber}}
        <div class="row gtr-uniform">
            <div class="col-3 col-6-xsmall">
                    <input type="text" name="demo-name" id="move-number" value="" placeholder="이동할 페이지 번호를 입력">
            </div>
            <div class="col-3 col-6-xsmall">
                <input type="text" value="{{pageCount}}" readonly>
            </div>
            <div class="col-12">
                <ul class="actions">
                    <li><input type="submit" value="페이지 이동" id="move-page" class="primary"></li>
                </ul>
            </div>
        </div>
    {{/pageCount}}
    <br/>
    <ul class="actions">
        <li><input type="button" value="단어 가리기" id = "btn-word" class="primary" /></li>
        <li><input type="button" value="의미 가리기" id = "btn-mean" class="primary" /></li>
    </ul>
    <ul class="actions">
        <li><a href="/api/words/save" class="button primary">단어 업로드</a></li>
    </ul>
    <ul class="actions">
        <li>
            <input type="file" accept="application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" id="file" required/>
        </li>
        <li>
            <input type="button" value="엑셀 업로드" id = "btn-excel" class="button icon solid fa-download" />
        </li>
        <li>
            <form action="/api/words/excelDown" method="post">
                <input type="submit" value="양식 다운로드" id = "btn-xlsdown" class="button icon solid fa-download" />
            </form>
        </li>
    </ul>
</div>
{{>layout/sidebar}}